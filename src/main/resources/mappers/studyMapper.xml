<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.javawspring.dao.StudyDAO">

	<select id="getGuestMid" resultType="com.spring.javawspring.vo.GuestVO">
		select * from guest2 where name = #{name};
	</select>
	
	<select id="getGuestNames" resultType="com.spring.javawspring.vo.GuestVO">
		select * from guest2 where ${search} like concat('%',#{name},'%');
	</select>
	
	<select id="getMemEmail" resultType="com.spring.javawspring.vo.MemberVO">
		select email from member2;
	</select>
	
	<select id="getQrCodeInfo" resultType="com.spring.javawspring.vo.qrCodeVO">
		select * from qrCode where idx =#{idx};
	</select>
	
	<insert id="setFoodInfo">
		insert into qrCode values(#{idx}, #{qrCode},#{name}, #{price}, #{ingredient} );
	</insert>
	
</mapper>